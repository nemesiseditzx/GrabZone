const C=window.GRABZONE_CONFIG;let sb=null;
if(C&&!C.supabaseUrl.includes("PASTE_")&&window.supabase)sb=window.supabase.createClient(C.supabaseUrl,C.supabaseAnonKey);
const $=id=>document.getElementById(id);
const fields=["store_name","tagline","hero_eyebrow","hero_title","hero_title_em","hero_description","hero_button_text","hero_button_link","how_button_text","how_button_link","offer_title","offer_message","offer_code","collection_eyebrow","collection_title","how_eyebrow","how_title","step1_title","step1_body","step2_title","step2_body","step3_title","step3_body","referral_eyebrow","referral_title","referral_body","referral_button_text","footer_text","whatsapp","instagram","messenger","header_link1_label","header_link1_url","header_link2_label","header_link2_url","header_link3_label","header_link3_url","custom_css"];
function esc(x){return String(x??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]))}
async function login(){if(!sb){$("loginMsg").textContent="Supabase is not configured.";return}const{error}=await sb.auth.signInWithPassword({email:$("email").value.trim(),password:$("password").value});$("loginMsg").textContent=error?error.message:"";if(!error)showApp()}
async function logout(){await sb.auth.signOut();location.reload()}
function showApp(){$("loginBox").classList.add("hidden");$("app").classList.remove("hidden");loadSettings();loadProducts();loadNotices()}
function previewFile(input,id){const f=input.files?.[0],box=$(id);if(!f||!box)return;box.innerHTML=`<img src="${URL.createObjectURL(f)}" alt="preview">`}
function previewMultiple(input){const files=[...input.files].slice(0,10),box=$("multiPreview");box.innerHTML="";if(!files.length){box.innerHTML='<div class="empty-preview">No images selected</div>';return}files.forEach((f,i)=>{const d=document.createElement("div");d.className="multi-thumb";d.innerHTML=`<img src="${URL.createObjectURL(f)}"><span>${i===0?"MAIN":i+1}</span>`;box.appendChild(d)})}
async function uploadImage(file){if(!file)throw new Error("Choose an image.");const ext=(file.name.split(".").pop()||"jpg").toLowerCase(),path=`${crypto.randomUUID()}.${ext}`;const{error}=await sb.storage.from("product-images").upload(path,file,{contentType:file.type,upsert:false});if(error)throw error;return sb.storage.from("product-images").getPublicUrl(path).data.publicUrl}
async function uploadImages(files){const out=[];for(const f of [...files].slice(0,10))out.push(await uploadImage(f));return out}
async function loadSettings(){const{data,error}=await sb.from("site_settings").select("*").eq("id",1).maybeSingle();if(error){$("settingsMsg").textContent=error.message;return}if(!data)return;fields.forEach(k=>{const e=$("s_"+k);if(e)e.value=data[k]??""});$("s_primary").value=data.primary_color||"#111111";$("s_bg").value=data.page_background||"#ffffff";["notice","offer","how"].forEach(k=>{$("s_show_"+k).value=String(data["show_"+k]??true)});$("s_show_ref").value=String(data.show_referral??true);if(data.logo_url)$("logoPreview").innerHTML=`<img src="${esc(data.logo_url)}">`;if(data.hero_image_url)$("heroPreview").innerHTML=`<img src="${esc(data.hero_image_url)}">`}
async function saveSettings(){try{const p={};fields.forEach(k=>{const e=$("s_"+k);if(e)p[k]=e.value});if($("s_logo").files[0])p.logo_url=await uploadImage($("s_logo").files[0]);if($("s_hero").files[0])p.hero_image_url=await uploadImage($("s_hero").files[0]);p.primary_color=$("s_primary").value;p.page_background=$("s_bg").value;p.show_notice=$("s_show_notice").value==="true";p.show_offer=$("s_show_offer").value==="true";p.show_how=$("s_show_how").value==="true";p.show_referral=$("s_show_ref").value==="true";p.updated_at=new Date().toISOString();const{error}=await sb.from("site_settings").upsert({id:1,...p},{onConflict:"id"});if(error)throw error;$("settingsMsg").textContent="✓ Saved. Refresh the storefront.";await loadSettings()}catch(e){$("settingsMsg").textContent="✕ "+e.message}}
async function addProduct(){try{const name=$("name").value.trim(),category=$("category").value.trim(),price=$("price").value,files=[...($("images").files||[])].slice(0,10);if(!name||!category||!price){$("productMsg").textContent="Enter product name, category and price.";return}if(!files.length){$("productMsg").textContent="Choose at least one product image.";return}$("productMsg").textContent=`Uploading ${files.length} image${files.length>1?"s":""}...`;const urls=await uploadImages(files);const{data:p,error}=await sb.from("products").insert({name,category,price:Number(price),old_price:$("oldPrice").value?Number($("oldPrice").value):null,image_url:urls[0],tag:$("tag").value.trim(),description:$("description").value.trim(),published:$("published").value==="true"}).select().single();if(error)throw error;const rows=urls.map((url,i)=>({product_id:p.id,image_url:url,sort_order:i,is_main:i===0}));const{error:ie}=await sb.from("product_images").insert(rows);if(ie)throw ie;$("productMsg").textContent=`✓ Product saved with ${urls.length} image${urls.length>1?"s":""}.`;["name","category","price","oldPrice","tag","description"].forEach(id=>$(id).value="");$("images").value="";$("multiPreview").innerHTML='<div class="empty-preview">No images selected</div>';loadProducts()}catch(e){$("productMsg").textContent="✕ "+e.message}}
async function loadProducts(){const{data,error}=await sb.from("products").select("*").order("created_at",{ascending:false});if(error){$("products").innerHTML=esc(error.message);return}$("statProducts").textContent=(data||[]).length;const html=[];for(const p of data||[]){const{data:imgs}=await sb.from("product_images").select("*").eq("product_id",p.id).order("sort_order");const count=imgs?.length||0;html.push(`<div class="item"><img src="${esc(p.image_url)}"><div><b>${esc(p.name)}</b><div class="meta">${esc(p.category)} · ৳${Number(p.price).toLocaleString()} · ${count||1} image${(count||1)>1?"s":""} · ${p.published?"Published":"Hidden"}</div></div><div><button class="ghost" onclick="toggleProduct('${p.id}',${p.published})">${p.published?"Hide":"Publish"}</button> <button class="ghost" onclick="deleteProduct('${p.id}')">Delete</button></div></div>`)}$("products").innerHTML=html.join("")||"<p>No products yet.</p>"}
async function toggleProduct(id,p){await sb.from("products").update({published:!p,updated_at:new Date().toISOString()}).eq("id",id);loadProducts()}
async function deleteProduct(id){if(confirm("Delete this product and its images?")){await sb.from("products").delete().eq("id",id);loadProducts()}}
async function addNotice(){const title=$("noticeTitle").value.trim(),message=$("noticeMessage").value.trim();if(!title||!message)return alert("Enter notice title and message.");const{error}=await sb.from("notices").insert({title,message,sort_order:Number($("noticeOrder").value||0),active:true});if(error)return alert(error.message);$("noticeTitle").value="";$("noticeMessage").value="";loadNotices()}
async function loadNotices(){const{data,error}=await sb.from("notices").select("*").order("sort_order");if(error){$("notices").innerHTML=esc(error.message);return}$("statNotices").textContent=(data||[]).filter(x=>x.active).length;$("notices").innerHTML=(data||[]).map(n=>`<div class="notice"><b>${esc(n.title)}</b><p>${esc(n.message)}</p><button class="ghost" onclick="toggleNotice('${n.id}',${n.active})">${n.active?"Hide":"Show"}</button> <button class="ghost" onclick="deleteNotice('${n.id}')">Delete</button></div>`).join("")||"<p>No notices yet.</p>"}
async function toggleNotice(id,p){await sb.from("notices").update({active:!p}).eq("id",id);loadNotices()}
async function deleteNotice(id){if(confirm("Delete this notice?")){await sb.from("notices").delete().eq("id",id);loadNotices()}}
function switchTab(tab){document.querySelectorAll(".tab").forEach(x=>x.classList.remove("active"));document.querySelectorAll(".side-link").forEach(x=>x.classList.remove("active"));$("tab-"+tab)?.classList.add("active");document.querySelector(`.side-link[data-tab="${tab}"]`)?.classList.add("active");scrollTo(0,0)}
document.addEventListener("click",e=>{const s=e.target.closest(".side-link");if(s)switchTab(s.dataset.tab);const q=e.target.closest("[data-tab-target]");if(q)switchTab(q.dataset.tabTarget)});
(async()=>{if(sb){const{data}=await sb.auth.getSession();if(data.session)showApp()}})();