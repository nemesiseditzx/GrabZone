(()=>{'use strict';
const C=window.GRABZONE_CONFIG||{},base=String(C.backendUrl||'').replace(/\/$/,'');
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function readJson(r){const text=await r.text();if(!text.trim())return null;try{return JSON.parse(text)}catch{return null}}
async function init(){if(document.getElementById('gzVendorDirectory'))return;try{const r=await fetch(base+'/api/marketplace/public',{cache:'no-store'}),x=await readJson(r);if(!r.ok||!x||!Array.isArray(x.vendors)||!x.vendors.length)return;const current=new URLSearchParams(location.search).get('vendor')||'';const section=document.createElement('section');section.id='gzVendorDirectory';section.className='gz-vendor-directory';section.innerHTML='<div class="gz-vendor-directory-head"><div><small>Marketplace</small><h2>Shop by brand</h2></div>'+(current?'<a href="./#shop">All products</a>':'')+'</div><div class="gz-vendor-grid">'+x.vendors.slice(0,12).map(v=>'<a class="gz-vendor-card '+(v.slug===current?'active':'')+'" href="?vendor='+encodeURIComponent(v.slug)+'#shop"><div class="gz-vendor-logo">'+(v.logo_url?'<img src="'+esc(v.logo_url)+'" alt="'+esc(v.name)+'">':esc(String(v.name||'V').slice(0,1).toUpperCase()))+'</div><b>'+esc(v.name)+'</b><span>'+esc(v.tagline||v.description||v.category||'Marketplace seller')+'</span><i>'+(v.slug===current?'Currently viewing':'View brand →')+'</i></a>').join('')+'</div>';const target=document.querySelector('.shop-section')||document.querySelector('#shop');if(target)target.parentNode.insertBefore(section,target);else document.body.appendChild(section)}catch(e){console.warn('GrabZone marketplace storefront:',e)}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,300));else setTimeout(init,300)})();
